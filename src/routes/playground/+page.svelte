<section class="page">
	<h1>Playground</h1>
	<p>Canvas, 3D, and SVG experiments.</p>
</section>

<style>
	.page {
		padding: var(--space-16) var(--space-8);
		max-width: var(--max-w-wide);
		margin: 0 auto;
	}

	h1 {
		font-size: var(--text-4xl);
		font-weight: 400;
		letter-spacing: -0.02em;
		margin-bottom: var(--space-4);
	}

	p {
		color: var(--color-text-muted);
	}
</style>
